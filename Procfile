# Procfile para Clever Cloud con Spring Boot

# Primero, hacemos el build del proyecto con Maven
build: mvn clean package -DskipTests

# Luego, arrancamos la aplicación con el JAR generado
web: java -jar target/adeline-0.0.1-SNAPSHOT.jar
